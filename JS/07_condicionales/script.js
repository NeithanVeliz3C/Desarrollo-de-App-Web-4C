function clima() {
    let temperatura = 23;
    if (temperatura <= 0) {
        alert("Hace frío.");
    } else if (temperatura >= 0 && temperatura <=25 ) {
        alert("La temperatura es agradable");
    }
}